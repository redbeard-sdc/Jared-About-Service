config:
  target: 'http://localhost:3001'
  # plugins:
  #   expect: {}
  phases:
    - duration: 10
      arrivalRate: 1000
scenarios:
  - flow:
    - get:
        url: "/api/hotel/200000"
        # expect:
        #   - statusCode: 200
        #   - contentType: json
